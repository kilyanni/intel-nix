diff --git a/libdevice/CMakeLists.txt b/libdevice/CMakeLists.txt
index 1a45e5a8f846..8fc1fa201969 100644
--- a/libdevice/CMakeLists.txt
+++ b/libdevice/CMakeLists.txt
@@ -43,6 +43,15 @@ else()
   endforeach()
 endif()
 
+include(GNUInstallDirs)
+if(LLVM_MAIN_SRC_DIR)
+  # In-tree build: preserve LLVM's lib${LLVM_LIBDIR_SUFFIX} install
+  # convention exactly (the clang driver hardcodes it when locating device
+  # libraries, and GNUInstallDirs may default CMAKE_INSTALL_LIBDIR to e.g.
+  # lib64). A normal variable shadows the cache entry for this scope only.
+  set(CMAKE_INSTALL_LIBDIR "lib${LLVM_LIBDIR_SUFFIX}")
+endif()
+
 # Build libdevice for SYCL.
 include(SYCLLibdevice)
 
diff --git a/libdevice/cmake/modules/SYCLLibdevice.cmake b/libdevice/cmake/modules/SYCLLibdevice.cmake
index 4f7d06e668ba..4c8e361184ad 100644
--- a/libdevice/cmake/modules/SYCLLibdevice.cmake
+++ b/libdevice/cmake/modules/SYCLLibdevice.cmake
@@ -6,14 +6,17 @@ else()
   set(devicelib_host_static_obj libsycl-devicelib-host.a)
 endif()
 set(bc-suffix bc)
-set(install_dest_obj lib${LLVM_LIBDIR_SUFFIX})
+set(install_dest_obj ${CMAKE_INSTALL_LIBDIR})
 
 if(WIN32)
   # On Windows, install to lib/
-  set(install_dest_bc lib${LLVM_LIBDIR_SUFFIX})
+  set(install_dest_bc ${CMAKE_INSTALL_LIBDIR})
   set(bc_binary_dir "${CMAKE_LIBRARY_OUTPUT_DIRECTORY}")
 else()
-  # On other platforms, install to lib/dpcpp-<major_ver>/sycl
+  # On other platforms, install to lib${LLVM_LIBDIR_SUFFIX}/dpcpp-<major_ver>/sycl.
+  # Deliberately not CMAKE_INSTALL_LIBDIR-based: the clang driver locates
+  # device libraries via CLANG_INSTALL_LIBDIR_BASENAME (lib${LLVM_LIBDIR_SUFFIX}),
+  # which need not match an arbitrary GNUInstallDirs CMAKE_INSTALL_LIBDIR.
   set(bc_dir "lib${LLVM_LIBDIR_SUFFIX}/dpcpp-${DPCPP_VERSION_MAJOR}/sycl")
   set(install_dest_bc ${bc_dir})
   set(bc_binary_dir "${CMAKE_BINARY_DIR}/${bc_dir}")
diff --git a/llvm-spirv/CMakeLists.txt b/llvm-spirv/CMakeLists.txt
index 59e5ea0b7072..0e82ed2242d0 100644
--- a/llvm-spirv/CMakeLists.txt
+++ b/llvm-spirv/CMakeLists.txt
@@ -22,6 +22,12 @@ endif (NOT DEFINED LLVM_SPIRV_BUILD_EXTERNAL)
 if(LLVM_SPIRV_BUILD_EXTERNAL)
   # Make sure llvm-spirv gets built when building outside the llvm tree.
   set(LLVM_BUILD_TOOLS ON)
+else()
+  # In-tree build: preserve LLVM's lib${LLVM_LIBDIR_SUFFIX} install
+  # convention exactly (add_llvm_library() installs libLLVMSPIRVLib there
+  # unconditionally, and GNUInstallDirs may default CMAKE_INSTALL_LIBDIR to
+  # e.g. lib64). A normal variable shadows the cache entry for this scope.
+  set(CMAKE_INSTALL_LIBDIR "lib${LLVM_LIBDIR_SUFFIX}")
 endif(LLVM_SPIRV_BUILD_EXTERNAL)
 
 # Download spirv.hpp from the official SPIRV-Headers repository.
@@ -164,13 +170,23 @@ install(
     ${LLVM_SPIRV_INCLUDE_DIRS}/LLVMSPIRVOpts.h
     ${LLVM_SPIRV_INCLUDE_DIRS}/LLVMSPIRVExtensions.inc
   DESTINATION
-    ${CMAKE_INSTALL_PREFIX}/include/LLVMSPIRVLib
+    ${CMAKE_INSTALL_INCLUDEDIR}/LLVMSPIRVLib
 )
 
+# libLLVMSPIRVLib is installed by add_llvm_library() to
+# lib${LLVM_LIBDIR_SUFFIX} in both in-tree and standalone builds, so the .pc
+# libdir is spelled with @LLVM_LIBDIR_SUFFIX@ in the template. The headers
+# however follow GNUInstallDirs in standalone builds; in-tree the historical
+# .pc contents are preserved byte-for-byte.
+if(LLVM_SPIRV_BUILD_EXTERNAL)
+  set(LLVM_SPIRV_PC_INCLUDEDIR "${CMAKE_INSTALL_FULL_INCLUDEDIR}")
+else()
+  set(LLVM_SPIRV_PC_INCLUDEDIR "\${prefix}/include")
+endif()
 configure_file(LLVMSPIRVLib.pc.in ${CMAKE_BINARY_DIR}/LLVMSPIRVLib.pc @ONLY)
 install(
   FILES
     ${CMAKE_BINARY_DIR}/LLVMSPIRVLib.pc
   DESTINATION
-    ${CMAKE_INSTALL_PREFIX}/lib${LLVM_LIBDIR_SUFFIX}/pkgconfig
+    ${CMAKE_INSTALL_LIBDIR}/pkgconfig
 )
diff --git a/llvm-spirv/LLVMSPIRVLib.pc.in b/llvm-spirv/LLVMSPIRVLib.pc.in
index 814c59251611..3dbe398d0d7a 100644
--- a/llvm-spirv/LLVMSPIRVLib.pc.in
+++ b/llvm-spirv/LLVMSPIRVLib.pc.in
@@ -1,7 +1,7 @@
 prefix=@CMAKE_INSTALL_PREFIX@
 exec_prefix=${prefix}
 libdir=${prefix}/lib@LLVM_LIBDIR_SUFFIX@
-includedir=${prefix}/include
+includedir=@LLVM_SPIRV_PC_INCLUDEDIR@
 
 Name: LLVMSPIRVLib
 Description: LLVM/SPIR-V bi-directional translator
diff --git a/llvm/include/llvm/SYCLLowerIR/CMakeLists.txt b/llvm/include/llvm/SYCLLowerIR/CMakeLists.txt
index fee74421f102..99c7f2920a9b 100644
--- a/llvm/include/llvm/SYCLLowerIR/CMakeLists.txt
+++ b/llvm/include/llvm/SYCLLowerIR/CMakeLists.txt
@@ -8,5 +8,5 @@ tablegen(LLVM DeviceConfigFile.inc -gen-dynamic-tables)
 add_public_tablegen_target(DeviceConfigFile)
 install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/DeviceConfigFile.hpp"
               "${CMAKE_CURRENT_BINARY_DIR}/DeviceConfigFile.inc"
-        DESTINATION include/llvm/SYCLLowerIR
+        DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/llvm/SYCLLowerIR
         COMPONENT DeviceConfigFile)
diff --git a/llvm/lib/ExecutionEngine/Interpreter/CMakeLists.txt b/llvm/lib/ExecutionEngine/Interpreter/CMakeLists.txt
index 8046bca2b1a4..997bd51e2b10 100644
--- a/llvm/lib/ExecutionEngine/Interpreter/CMakeLists.txt
+++ b/llvm/lib/ExecutionEngine/Interpreter/CMakeLists.txt
@@ -21,4 +21,4 @@ configure_file(
   Interpreter.h
   ${LLVM_BINARY_DIR}/include/llvm/ExecutionEngine/Interpreter/Interpreter.h)
 
-install(FILES Interpreter.h DESTINATION include/llvm/ExecutionEngine/Interpreter)
+install(FILES Interpreter.h DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}/llvm/ExecutionEngine/Interpreter)
diff --git a/sycl/CMakeLists.txt b/sycl/CMakeLists.txt
index 9d11782674dd..a12979e198e9 100644
--- a/sycl/CMakeLists.txt
+++ b/sycl/CMakeLists.txt
@@ -1,6 +1,26 @@
 cmake_minimum_required(VERSION 3.20.0)
 
 project(sycl-solution)
+
+include(GNUInstallDirs)
+
+# Standalone builds honour GNUInstallDirs (e.g. CMAKE_INSTALL_LIBDIR=lib64 or
+# a value set explicitly by distro tooling). In-tree builds must preserve
+# LLVM's lib${LLVM_LIBDIR_SUFFIX} convention exactly: the clang driver and
+# AddLLVM RPATHs hardcode it, while GNUInstallDirs may default
+# CMAKE_INSTALL_LIBDIR to e.g. lib64. Deliberately NOT shadowing
+# CMAKE_INSTALL_LIBDIR itself: pre-existing uses below this scope
+# (sycl/tools/sycl-prof|sycl-trace|sycl-sanitize, unified-runtime) already
+# follow CMAKE_INSTALL_LIBDIR when building in-tree.
+if(LLVM_MAIN_SRC_DIR)
+  set(SYCL_INSTALL_LIBDIR "lib${LLVM_LIBDIR_SUFFIX}")
+  set(SYCL_INSTALL_FULL_LIBDIR "${CMAKE_INSTALL_PREFIX}/${SYCL_INSTALL_LIBDIR}")
+  set(SYCL_INSTALL_FULL_BINDIR "${CMAKE_INSTALL_PREFIX}/bin")
+else()
+  set(SYCL_INSTALL_LIBDIR "${CMAKE_INSTALL_LIBDIR}")
+  set(SYCL_INSTALL_FULL_LIBDIR "${CMAKE_INSTALL_FULL_LIBDIR}")
+  set(SYCL_INSTALL_FULL_BINDIR "${CMAKE_INSTALL_FULL_BINDIR}")
+endif()
 # Requirements
 set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
@@ -348,16 +368,24 @@ if (NOT WIN32)
   install(FILES
     "${CMAKE_CURRENT_SOURCE_DIR}/gdb/libsycl.so-gdb.py"
     RENAME "libsycl.so.${SYCL_VERSION_STRING}-gdb.py"
-    DESTINATION "lib${LLVM_LIBDIR_SUFFIX}/"
+    DESTINATION "${SYCL_INSTALL_LIBDIR}/"
     COMPONENT sycl-headers-extras)
   if (SYCL_ENABLE_MAJOR_RELEASE_PREVIEW_LIB)
     install(FILES
       "${CMAKE_CURRENT_SOURCE_DIR}/gdb/libsycl.so-gdb.py"
       RENAME "libsycl-preview.so.${SYCL_VERSION_STRING}-gdb.py"
-      DESTINATION "lib${LLVM_LIBDIR_SUFFIX}/"
+      DESTINATION "${SYCL_INSTALL_LIBDIR}/"
       COMPONENT sycl-headers-extras)
   endif()
 
+  # In-tree: keep the historical pkg-config contents byte-for-byte
+  # (libdir relative to ${prefix}); standalone: advertise the real
+  # GNUInstallDirs location libsycl is installed to.
+  if(LLVM_MAIN_SRC_DIR)
+    set(SYCL_PC_LIBDIR "\${prefix}/lib${LLVM_LIBDIR_SUFFIX}")
+  else()
+    set(SYCL_PC_LIBDIR "${CMAKE_INSTALL_FULL_LIBDIR}")
+  endif()
   configure_file(
     "${CMAKE_CURRENT_SOURCE_DIR}/pkgconfig/sycl.pc.in"
     "${CMAKE_CURRENT_BINARY_DIR}/sycl-dpcpp-${DPCPP_VERSION_MAJOR}.pc"
@@ -365,7 +393,7 @@ if (NOT WIN32)
 
   install(
     FILES "${CMAKE_CURRENT_BINARY_DIR}/sycl-dpcpp-${DPCPP_VERSION_MAJOR}.pc"
-    DESTINATION "lib${LLVM_LIBDIR_SUFFIX}/pkgconfig"
+    DESTINATION "${SYCL_INSTALL_LIBDIR}/pkgconfig"
     COMPONENT sycl)
 endif()
 
diff --git a/sycl/cmake/modules/BuildUnifiedRuntime.cmake b/sycl/cmake/modules/BuildUnifiedRuntime.cmake
index 646ca364a10f..888084a3f473 100644
--- a/sycl/cmake/modules/BuildUnifiedRuntime.cmake
+++ b/sycl/cmake/modules/BuildUnifiedRuntime.cmake
@@ -112,9 +112,9 @@ find_package(Threads REQUIRED)
 if(TARGET UnifiedRuntimeLoader)
   # Install the UR loader.
   install(TARGETS ur_loader
-    LIBRARY DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT unified-runtime-loader
-    ARCHIVE DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT unified-runtime-loader
-    RUNTIME DESTINATION "bin" COMPONENT unified-runtime-loader
+    LIBRARY DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT unified-runtime-loader
+    ARCHIVE DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT unified-runtime-loader
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT unified-runtime-loader
   )
 endif()
 
@@ -124,8 +124,8 @@ function(add_sycl_ur_adapter NAME)
   add_dependencies(UnifiedRuntimeAdapters ur_adapter_${NAME})
 
   install(TARGETS ur_adapter_${NAME}
-    LIBRARY DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT ur_adapter_${NAME}
-    RUNTIME DESTINATION "bin" COMPONENT ur_adapter_${NAME})
+    LIBRARY DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT ur_adapter_${NAME}
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT ur_adapter_${NAME})
 
   set(manifest_file
     ${CMAKE_CURRENT_BINARY_DIR}/install_manifest_ur_adapter_${NAME}.txt)
@@ -278,24 +278,24 @@ if(CMAKE_SYSTEM_NAME STREQUAL Windows)
   # Add the debug UR runtime libraries to the parent install.
   install(
     FILES ${URD_INSTALL_DIR}/bin/ur_loaderd.dll
-    DESTINATION "bin" COMPONENT unified-runtime-loader)
+    DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT unified-runtime-loader)
   foreach(adapter ${SYCL_ENABLE_BACKENDS})
     install(
       FILES ${URD_INSTALL_DIR}/bin/ur_adapter_${adapter}d.dll
-      DESTINATION "bin" COMPONENT ur_adapter_${adapter})
+      DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT ur_adapter_${adapter})
     add_dependencies(install-sycl-ur-adapter-${adapter} unified-runtimed)
   endforeach()
   if(UMF_BUILD_SHARED_LIBRARY)
     # Also install umfd.dll
     install(
       FILES ${URD_INSTALL_DIR}/bin/umfd.dll
-      DESTINATION "bin" COMPONENT unified-memory-framework)
+      DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT unified-memory-framework)
   endif()
 endif()
 
 if(TARGET umf)
   install(TARGETS umf
-    LIBRARY DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT unified-memory-framework
-    ARCHIVE DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT unified-memory-framework
-    RUNTIME DESTINATION "bin" COMPONENT unified-memory-framework)
+    LIBRARY DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT unified-memory-framework
+    ARCHIVE DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT unified-memory-framework
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT unified-memory-framework)
 endif()
diff --git a/sycl/cmake/modules/SYCLUtils.cmake b/sycl/cmake/modules/SYCLUtils.cmake
index f469b2604760..a8728bdfb5b1 100644
--- a/sycl/cmake/modules/SYCLUtils.cmake
+++ b/sycl/cmake/modules/SYCLUtils.cmake
@@ -13,7 +13,7 @@ macro(add_stripped_pdb ARG_TARGET_NAME)
     target_link_options(${ARG_TARGET_NAME}
                         PRIVATE "LINKER:/PDBSTRIPPED:${ARG_TARGET_NAME}.stripped.pdb")
     install(FILES "${CMAKE_CURRENT_BINARY_DIR}/${ARG_TARGET_NAME}.stripped.pdb"
-            DESTINATION ${CMAKE_INSTALL_PREFIX}/bin
+            DESTINATION ${CMAKE_INSTALL_BINDIR}
             RENAME "${ARG_TARGET_NAME}.pdb"
             COMPONENT ${ARG_TARGET_NAME}
             OPTIONAL)
diff --git a/sycl/pkgconfig/sycl.pc.in b/sycl/pkgconfig/sycl.pc.in
index 25df8878d76f..cbc01c4d86fe 100644
--- a/sycl/pkgconfig/sycl.pc.in
+++ b/sycl/pkgconfig/sycl.pc.in
@@ -1,6 +1,6 @@
 prefix=${pcfiledir}/../..
 includedir=${prefix}/@SYCL_INCLUDE_DIR@
-libdir=${prefix}/lib@LLVM_LIBDIR_SUFFIX@
+libdir=@SYCL_PC_LIBDIR@
 sycl_version=@SYCL_VERSION_STRING@
 
 Name: sycl-dpcpp-@DPCPP_VERSION_MAJOR@
diff --git a/sycl/source/CMakeLists.txt b/sycl/source/CMakeLists.txt
index 40327b594678..00174f606874 100644
--- a/sycl/source/CMakeLists.txt
+++ b/sycl/source/CMakeLists.txt
@@ -198,7 +198,7 @@ function(add_sycl_rt_library LIB_NAME LIB_OBJ_NAME)
         COMMENT "Creating version-agnostic copy of the import library.")
       install(
         FILES ${CMAKE_LIBRARY_OUTPUT_DIRECTORY}/${ARG_IMPLIB_NAME}.lib
-        DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT sycl)
+        DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT sycl)
     endif()
   endif()
 
@@ -436,6 +436,6 @@ if (WIN32)
 endif()
 
 install(TARGETS ${SYCL_RT_LIBS}
-  ARCHIVE DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT sycl
-  LIBRARY DESTINATION "lib${LLVM_LIBDIR_SUFFIX}" COMPONENT sycl
-  RUNTIME DESTINATION "bin" COMPONENT sycl)
+  ARCHIVE DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT sycl
+  LIBRARY DESTINATION ${SYCL_INSTALL_LIBDIR} COMPONENT sycl
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT sycl)
diff --git a/sycl/test/CMakeLists.txt b/sycl/test/CMakeLists.txt
index 4e7607e74931..d826ea9db213 100644
--- a/sycl/test/CMakeLists.txt
+++ b/sycl/test/CMakeLists.txt
@@ -8,7 +8,7 @@ set(LLVM_BUILD_BINARY_DIRS "${LLVM_BINARY_DIR}/bin/")
 set(LLVM_BUILD_LIBRARY_DIRS "${LLVM_BINARY_DIR}/lib/")
 
 set(RT_TEST_ARGS ${RT_TEST_ARGS} "-v")
-set(DEPLOY_RT_TEST_ARGS ${DEPLOY_RT_TEST_ARGS} "-v -D SYCL_TOOLS_DIR=${CMAKE_INSTALL_PREFIX}/bin -D SYCL_LIBS_DIR=${CMAKE_INSTALL_PREFIX}/lib${LLVM_LIBDIR_SUFFIX} -D SYCL_INCLUDE=${CMAKE_INSTALL_PREFIX}/${SYCL_INCLUDE_DIR}")
+set(DEPLOY_RT_TEST_ARGS ${DEPLOY_RT_TEST_ARGS} "-v -D SYCL_TOOLS_DIR=${SYCL_INSTALL_FULL_BINDIR} -D SYCL_LIBS_DIR=${SYCL_INSTALL_FULL_LIBDIR} -D SYCL_INCLUDE=${CMAKE_INSTALL_PREFIX}/${SYCL_INCLUDE_DIR}")
 
 find_package(Threads REQUIRED)
 set(SYCL_THREADS_LIB ${CMAKE_THREAD_LIBS_INIT})
diff --git a/sycl/tools/sycl-ls/CMakeLists.txt b/sycl/tools/sycl-ls/CMakeLists.txt
index a1cb33c6553b..7d93fbb79144 100644
--- a/sycl/tools/sycl-ls/CMakeLists.txt
+++ b/sycl/tools/sycl-ls/CMakeLists.txt
@@ -26,4 +26,4 @@ if (WIN32)
   target_link_options(sycl-ls PRIVATE LINKER:/DEPENDENTLOADFLAG:0x900)
 endif()
 install(TARGETS sycl-ls
-  RUNTIME DESTINATION "bin" COMPONENT sycl-ls)
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT sycl-ls)
diff --git a/sycl/tools/syclbin-dump/CMakeLists.txt b/sycl/tools/syclbin-dump/CMakeLists.txt
index d1eb71823f64..9bf370bdcbe4 100644
--- a/sycl/tools/syclbin-dump/CMakeLists.txt
+++ b/sycl/tools/syclbin-dump/CMakeLists.txt
@@ -22,4 +22,4 @@ endif()
 add_dependencies(sycl-toolchain syclbin-dump)
 
 install(TARGETS syclbin-dump
-  RUNTIME DESTINATION "bin" COMPONENT syclbin-dump)
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT syclbin-dump)
diff --git a/sycl/ur_win_proxy_loader/CMakeLists.txt b/sycl/ur_win_proxy_loader/CMakeLists.txt
index 740a764269ea..dc8b98f83098 100644
--- a/sycl/ur_win_proxy_loader/CMakeLists.txt
+++ b/sycl/ur_win_proxy_loader/CMakeLists.txt
@@ -14,7 +14,7 @@ configure_file(../../llvm/resources/windows_version_resource.rc ${CMAKE_CURRENT_
 add_library(ur_win_proxy_loader SHARED  ur_win_proxy_loader.cpp  ${CMAKE_CURRENT_BINARY_DIR}/versioninfo.rc)
 add_dependencies(ur_win_proxy_loader UnifiedRuntimeLoader)
 install(TARGETS ur_win_proxy_loader
-  RUNTIME DESTINATION "bin" COMPONENT ur_win_proxy_loader
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT ur_win_proxy_loader
 )
 
 if (MSVC)
@@ -65,5 +65,5 @@ if (MSVC)
   target_link_options(ur_win_proxy_loaderd PRIVATE LINKER:/DEPENDENTLOADFLAG:0x2000)
   target_link_options(ur_win_proxy_loader PRIVATE LINKER:/DEPENDENTLOADFLAG:0x2000)
   install(TARGETS ur_win_proxy_loaderd
-    RUNTIME DESTINATION "bin" COMPONENT ur_win_proxy_loader)
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT ur_win_proxy_loader)
 endif()
diff --git a/unified-runtime/CMakeLists.txt b/unified-runtime/CMakeLists.txt
index 2248cfcb3db2..7583ed1ae34a 100644
--- a/unified-runtime/CMakeLists.txt
+++ b/unified-runtime/CMakeLists.txt
@@ -363,7 +363,7 @@ target_include_directories(ur_headers INTERFACE
 # Add the include directory and the headers target to the install.
 install(
     DIRECTORY "${PROJECT_SOURCE_DIR}/include/"
-    DESTINATION include COMPONENT ur_headers)
+    DESTINATION ${CMAKE_INSTALL_INCLUDEDIR} COMPONENT ur_headers)
 install(
     TARGETS ur_headers
     EXPORT ${PROJECT_NAME}-targets)
@@ -379,14 +379,18 @@ if(UR_BUILD_TOOLS)
     add_subdirectory(tools)
 endif()
 
-# Add the list of installed targets to the install. This includes the namespace
-# which all installed targets will be prefixed with, e.g. for the headers
-# target users will depend on ${PROJECT_NAME}::headers.
-install(
-    EXPORT ${PROJECT_NAME}-targets
-    FILE ${PROJECT_NAME}-targets.cmake
-    NAMESPACE ${PROJECT_NAME}::
-    DESTINATION lib/cmake/${PROJECT_NAME})
+# The export set and package config files are only needed when UR is built as
+# a standalone project. When built in-tree (LLVM/SYCL), the
+# parent project provides its own install rules.
+if(UR_STANDALONE_BUILD)
+    # Add the list of installed targets to the install. This includes the
+    # namespace which all installed targets will be prefixed with, e.g. for
+    # the headers target users will depend on ${PROJECT_NAME}::headers.
+    install(
+        EXPORT ${PROJECT_NAME}-targets
+        FILE ${PROJECT_NAME}-targets.cmake
+        NAMESPACE ${PROJECT_NAME}::
+        DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/${PROJECT_NAME})
 
 # Configure the package versions file for use in find_package when installed.
 write_basic_package_version_file(
@@ -403,8 +407,15 @@ configure_package_config_file(
 install(
     FILES
         ${PROJECT_BINARY_DIR}/cmake/${PROJECT_NAME}-config.cmake
-        ${PROJECT_BINARY_DIR}/cmake/${PROJECT_NAME}-config-version.cmake
-    DESTINATION lib/cmake/${PROJECT_NAME})
+        INSTALL_DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/${PROJECT_NAME})
+
+    # Add the package files to the install.
+    install(
+        FILES
+            ${PROJECT_BINARY_DIR}/cmake/${PROJECT_NAME}-config.cmake
+            ${PROJECT_BINARY_DIR}/cmake/${PROJECT_NAME}-config-version.cmake
+        DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/${PROJECT_NAME})
+endif()
 
 set(API_JSON_FILE ${PROJECT_BINARY_DIR}/unified_runtime.json)
 
diff --git a/unified-runtime/source/loader/CMakeLists.txt b/unified-runtime/source/loader/CMakeLists.txt
index a331e10e34a8..4a99a845302c 100644
--- a/unified-runtime/source/loader/CMakeLists.txt
+++ b/unified-runtime/source/loader/CMakeLists.txt
@@ -102,11 +102,21 @@ if(WIN32)
 endif()
 
 if(UNIX)
+    if(LLVM_MAIN_SRC_DIR)
+        # In-tree intel/llvm build: preserve the historical pkg-config
+        # contents byte-for-byte (paths relative to ${prefix}).
+        set(UR_PC_INCLUDEDIR "\${prefix}/include")
+        set(UR_PC_LIBDIR "\${prefix}/lib")
+    else()
+        # Standalone build: advertise the real GNUInstallDirs locations.
+        set(UR_PC_INCLUDEDIR "${CMAKE_INSTALL_FULL_INCLUDEDIR}")
+        set(UR_PC_LIBDIR "${CMAKE_INSTALL_FULL_LIBDIR}")
+    endif()
     configure_file(
         ${CMAKE_CURRENT_SOURCE_DIR}/libur_loader.pc.in
         ${CMAKE_CURRENT_BINARY_DIR}/libur_loader.pc
         @ONLY)
-    install(FILES "${CMAKE_CURRENT_BINARY_DIR}/libur_loader.pc" DESTINATION "${CMAKE_INSTALL_FULL_LIBDIR}/pkgconfig" COMPONENT unified-runtime)
+    install(FILES "${CMAKE_CURRENT_BINARY_DIR}/libur_loader.pc" DESTINATION "${CMAKE_INSTALL_LIBDIR}/pkgconfig" COMPONENT unified-runtime)
 endif()
 
 target_sources(ur_loader
diff --git a/unified-runtime/source/loader/libur_loader.pc.in b/unified-runtime/source/loader/libur_loader.pc.in
index d79c3a9da8bd..dd6ed9657656 100644
--- a/unified-runtime/source/loader/libur_loader.pc.in
+++ b/unified-runtime/source/loader/libur_loader.pc.in
@@ -1,6 +1,6 @@
 prefix=@CMAKE_INSTALL_PREFIX@
-includedir=${prefix}/include
-libdir=${prefix}/lib
+includedir=@UR_PC_INCLUDEDIR@
+libdir=@UR_PC_LIBDIR@
 
 
 Name: Unified Runtime Loader
diff --git a/xpti/CMakeLists.txt b/xpti/CMakeLists.txt
index cc56bc669658..7ec464fe8d32 100644
--- a/xpti/CMakeLists.txt
+++ b/xpti/CMakeLists.txt
@@ -93,7 +93,13 @@ install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/include/xpti
 # Install CMake config files.
 include(CMakePackageConfigHelpers)
 set(INCLUDE_INSTALL_DIR ${CMAKE_INSTALL_INCLUDEDIR})
-set(LIB_INSTALL_DIR lib${LLVM_LIBDIR_SUFFIX})
+# In-tree: preserve LLVM's lib${LLVM_LIBDIR_SUFFIX} install convention;
+# standalone: honour GNUInstallDirs.
+if(LLVM_MAIN_SRC_DIR)
+  set(LIB_INSTALL_DIR lib${LLVM_LIBDIR_SUFFIX})
+else()
+  set(LIB_INSTALL_DIR ${CMAKE_INSTALL_LIBDIR})
+endif()
 set(CONFIG_INSTALL_DIR ${LIB_INSTALL_DIR}/cmake/xpti)
 
 configure_package_config_file(
diff --git a/xpti/src/CMakeLists.txt b/xpti/src/CMakeLists.txt
index 0f6692c18347..e0ccd17c54ed 100644
--- a/xpti/src/CMakeLists.txt
+++ b/xpti/src/CMakeLists.txt
@@ -1,4 +1,12 @@
 include(GNUInstallDirs)
+if(LLVM_MAIN_SRC_DIR)
+  # In-tree build: preserve LLVM's lib${LLVM_LIBDIR_SUFFIX} install
+  # convention exactly (GNUInstallDirs may default CMAKE_INSTALL_LIBDIR to
+  # e.g. lib64). A normal variable shadows the cache entry for this scope
+  # only; the parent scope makes the same in-tree/standalone distinction
+  # for LIB_INSTALL_DIR (cmake package/pkg-config dirs) explicitly.
+  set(CMAKE_INSTALL_LIBDIR "lib${LLVM_LIBDIR_SUFFIX}")
+endif()
 
 macro(add_xpti_lib target_name)
   add_library(${target_name} STATIC ${ARGN})
@@ -22,9 +30,9 @@ macro(add_xpti_lib target_name)
   # Set the location of the library installation.
   install(TARGETS ${target_name}
     EXPORT xptiTargets
-    RUNTIME DESTINATION bin COMPONENT xpti
-    LIBRARY DESTINATION lib${LLVM_LIBDIR_SUFFIX} COMPONENT xpti
-    ARCHIVE DESTINATION lib${LLVM_LIBDIR_SUFFIX} COMPONENT xpti
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT xpti
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR} COMPONENT xpti
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR} COMPONENT xpti
   )
 endmacro()
 
diff --git a/xptifw/CMakeLists.txt b/xptifw/CMakeLists.txt
index 78549309cafe..10390f0ce746 100644
--- a/xptifw/CMakeLists.txt
+++ b/xptifw/CMakeLists.txt
@@ -79,7 +79,13 @@ endif()
 # Install CMake config files
 include(CMakePackageConfigHelpers)
 set(INCLUDE_INSTALL_DIR ${CMAKE_INSTALL_INCLUDEDIR})
-set(LIB_INSTALL_DIR lib${LLVM_LIBDIR_SUFFIX})
+# In-tree: preserve LLVM's lib${LLVM_LIBDIR_SUFFIX} install convention;
+# standalone: honour GNUInstallDirs.
+if(LLVM_MAIN_SRC_DIR)
+  set(LIB_INSTALL_DIR lib${LLVM_LIBDIR_SUFFIX})
+else()
+  set(LIB_INSTALL_DIR ${CMAKE_INSTALL_LIBDIR})
+endif()
 set(CONFIG_INSTALL_DIR ${LIB_INSTALL_DIR}/cmake/xptifw)
 
 configure_package_config_file(
diff --git a/xptifw/src/CMakeLists.txt b/xptifw/src/CMakeLists.txt
index 5e865aa3714e..996951828275 100644
--- a/xptifw/src/CMakeLists.txt
+++ b/xptifw/src/CMakeLists.txt
@@ -1,6 +1,16 @@
 # Fetch third party headers
 include(FetchContent)
 
+if(LLVM_MAIN_SRC_DIR)
+  # In-tree build: preserve LLVM's lib${LLVM_LIBDIR_SUFFIX} install
+  # convention exactly (GNUInstallDirs may default CMAKE_INSTALL_LIBDIR to
+  # e.g. lib64). A normal variable shadows the cache entry for this scope
+  # only (add_xpti_library() below reads it); the parent scope makes the
+  # same in-tree/standalone distinction for LIB_INSTALL_DIR (cmake
+  # package/pkg-config dirs) explicitly.
+  set(CMAKE_INSTALL_LIBDIR "lib${LLVM_LIBDIR_SUFFIX}")
+endif()
+
 # The sycl target should download or find emhash but may not
 # for standalone builds.
 if (NOT TARGET emhash::emhash)
@@ -93,9 +103,9 @@ function(add_xpti_library LIB_NAME)
   include(GNUInstallDirs)
   install(TARGETS ${LIB_NAME}
     EXPORT xptifwTargets
-    RUNTIME DESTINATION bin COMPONENT xptifw
-    LIBRARY DESTINATION lib${LLVM_LIBDIR_SUFFIX} COMPONENT xptifw
-    ARCHIVE DESTINATION lib${LLVM_LIBDIR_SUFFIX} COMPONENT xptifw
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT xptifw
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR} COMPONENT xptifw
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR} COMPONENT xptifw
   )
 endfunction(add_xpti_library)
 
